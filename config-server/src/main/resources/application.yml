server:
  port: 8888

spring:
  profiles:
    active: native #Usar perfil nativo de spring boot
  application:
    name: config-server
  cloud:
    config:
      server:
        native:
          search-locations: classpath:/configurations #Ruta de configuracion de todos los microservicios

